spring:
  application:
    name: product-service

  config:
    import: configserver:http://localhost:8888

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis
    redis:
      time-to-live: 300000
      cache-null-values: false